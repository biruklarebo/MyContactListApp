<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mycontactlist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/contactlist_launch_imagehdpi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/contactlist_launch_imagehdpi"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyContactList">
        <activity
            android:name=".ContactMapActivity"
            android:exported="false" />
        <activity
            android:name=".ContactSettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ContactListActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>